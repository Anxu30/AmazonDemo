<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Address Info</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

<div class="flex justify-center items-center min-h-screen bg-gradient-to-br from-indigo-200 via-blue-100 to-purple-200 py-10 ">
  <div class="bg-white bg-opacity-90 rounded-3xl shadow-2xl p-8 w-full max-w-md relative overflow-hidden ">
    <div class="absolute -top-10 -right-10 w-32 h-32 bg-gradient-to-tr from-indigo-400 to-purple-300 opacity-20 rounded-full z-0"></div>
    <div class="absolute -bottom-10 -left-10 w-32 h-32 bg-gradient-to-bl from-blue-300 to-indigo-200 opacity-20 rounded-full z-0"></div>
    <div class="flex flex-col items-center gap-4 z-10 relative">
    
      <h2 class="text-2xl font-extrabold text-indigo-700 mb-1"><%= meta && meta.street ? meta.street : 'Street' %></h2>
      <div class="w-full flex flex-col gap-2 text-indigo-700 text-base">
        <div class="flex items-center gap-2 justify-center">
          <span class="font-semibold">City:</span>
          <span class="text-indigo-600"><%= meta && meta.city ? meta.city : 'Not Provided' %></span>
        </div>
        <div class="flex items-center gap-2 justify-center">
          <span class="font-semibold">State:</span>
          <span class="text-indigo-600"><%= meta && meta.state ? meta.state : 'Not Provided' %></span>
        </div>
        <div class="flex items-center gap-2 justify-center">
          <span class="font-semibold">ZipCode:</span>
          <span class="text-indigo-600"><%= meta && meta.pincode ? meta.pincode : 'Not Provided' %></span>
        </div>
        <div class="flex items-center gap-2 justify-center">
          <span class="font-semibold">Country:</span>
          <span class="text-indigo-600"><%= meta && meta.country ? meta.country : 'Not Provided' %></span>
        </div>
        
      </div>
      <div class="flex gap-4 mt-6 w-full">
        <a href="/address/<%= meta && meta.email ? meta.email : '' %>" class="flex-1 bg-gradient-to-r from-indigo-500 to-purple-400 text-white font-bold py-2 rounded-xl shadow-md hover:from-indigo-600 hover:to-purple-500 transition text-center">
          Edit
        </a>
        <a href="/" class="flex-1 bg-gradient-to-r from-gray-200 to-indigo-100 text-indigo-700 font-semibold py-2 rounded-xl shadow hover:from-indigo-100 hover:to-gray-200 transition text-center">
          Home
        </a>
      </div>
    </div>
  </div>
</div>
</div>

</body>
</html>