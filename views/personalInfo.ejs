<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Personal Info</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

<div class="flex justify-center items-center min-h-screen bg-gradient-to-br from-indigo-200 via-blue-100 to-purple-200 py-10 ">
  <div class="bg-white bg-opacity-90 rounded-3xl shadow-2xl p-8 w-full max-w-md relative overflow-hidden ">
    <div class="absolute -top-10 -right-10 w-32 h-32 bg-gradient-to-tr from-indigo-400 to-purple-300 opacity-20 rounded-full z-0"></div>
    <div class="absolute -bottom-10 -left-10 w-32 h-32 bg-gradient-to-bl from-blue-300 to-indigo-200 opacity-20 rounded-full z-0"></div>
    <div class="flex flex-col items-center gap-4 z-10 relative">
      <% if (meta && meta.profilePicUrl) { %>
        <img src="<%= meta.profilePicUrl %>" alt="Profile Picture" class="w-32 h-32 rounded-full object-cover border-4 border-indigo-200 shadow-md mb-2" />
      <% } else { %>
        <div class="w-32 h-32 rounded-full bg-gradient-to-br from-indigo-100 to-purple-100 flex items-center justify-center text-5xl text-indigo-400 border-4 border-indigo-100 shadow mb-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A13.937 13.937 0 0112 15c2.5 0 4.847.655 6.879 1.804M15 10a3 3 0 11-6 0 3 3 0 016 0z" />
          </svg>
        </div>
      <% } %>
      <h2 class="text-2xl font-extrabold text-indigo-700 mb-1"><%= meta && meta.name ? meta.name : 'Your Name' %></h2>
      <div class="w-full flex flex-col gap-2 text-indigo-700 text-base">
        <div class="flex items-center gap-2 justify-center">
          <span class="font-semibold">Email:</span>
          <span class="text-indigo-600"><%= meta && meta.email ? meta.email : 'Not Provided' %></span>
        </div>
        <div class="flex items-center gap-2 justify-center">
          <span class="font-semibold">Contact:</span>
          <span class="text-indigo-600"><%= meta && meta.contact ? meta.contact : 'Not Provided' %></span>
        </div>
        <div class="flex items-center gap-2 justify-center">
          <span class="font-semibold">DOB:</span>
          <span class="text-indigo-600"><%= meta && meta.DOB ? meta.DOB : 'Not Provided' %></span>
        </div>
        
      </div>
      <div class="flex gap-4 mt-6 w-full">
        <a href="/personal/<%= meta && meta.email ? meta.email : '' %>" class="flex-1 bg-gradient-to-r from-indigo-500 to-purple-400 text-white font-bold py-2 rounded-xl shadow-md hover:from-indigo-600 hover:to-purple-500 transition text-center">
          Edit
        </a>
        <a href="/" class="flex-1 bg-gradient-to-r from-gray-200 to-indigo-100 text-indigo-700 font-semibold py-2 rounded-xl shadow hover:from-indigo-100 hover:to-gray-200 transition text-center">
          Home
        </a>
      </div>
    </div>
  </div>
</div>
</div>

</body>
</html>