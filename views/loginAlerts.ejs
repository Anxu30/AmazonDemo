<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login Alert Settings</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f7f7f7;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 400px;
      margin: 60px auto;
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      padding: 32px 24px;
    }
    h2 {
      margin-top: 0;
      color: #333;
      text-align: center;
    }
    .info {
      background: #eaf6ff;
      color: #3178c6;
      padding: 12px;
      border-radius: 4px;
      margin-bottom: 24px;
      font-size: 15px;
      text-align: center;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }
    label {
      font-size: 14px;
      color: #444;
      margin-bottom: 4px;
    }
    input[type="email"], input[type="password"] {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 15px;
    }
    button {
      background: #3178c6;
      color: #fff;
      border: none;
      padding: 12px;
      border-radius: 4px;
      font-size: 16px;
      cursor: pointer;
      margin-top: 8px;
      transition: background 0.2s;
    }
    button:hover {
      background: #255a99;
    }
    .alert-setting {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-top: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Login</h2>
    <div class="info">
      Get notified whenever a login is detected from a new device or location.
    </div>
    <form action="/login" method="POST">
      <div>
        <label for="email">Email</label>
        <input type="email" id="email" name="email" required autocomplete="username">
      </div>
      <div>
        <label for="password">Password</label>
        <input type="password" id="password" name="password" required autocomplete="current-password">
      </div>
      <div class="alert-setting">
        <input type="checkbox" id="notify" name="notify" checked>
        <label for="notify">Send me an alert if a login is detected from a new device or location</label>
      </div>
      <button type="submit">Login</button>
    </form>
  </div>
</body>
</html>

