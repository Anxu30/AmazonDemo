<!DOCTYPE html>
<html>
<head>
    <title>Update Email</title>
    
</head>
<body>
    <style>
      .update-email-card {
        max-width: 370px;
        margin: 60px auto 0 auto;
        background: #fff;
        border-radius: 10px;
        box-shadow: 0 2px 16px rgba(0,0,0,0.13);
        padding: 32px 28px 24px 28px;
        display: flex;
        flex-direction: column;
        align-items: stretch;
      }
      .update-email-card h2 {
        text-align: center;
        margin-bottom: 18px;
        color: #222;
        font-size: 1.5rem;
        font-weight: 700;
      }
      .update-email-card label {
        margin-bottom: 7px;
        font-size: 1.04rem;
        color: #444;
        font-weight: 500;
      }
      .update-email-card input[type="email"] {
        width: 100%;
        padding: 10px 12px;
        border: 1px solid #bbb;
        border-radius: 5px;
        margin-bottom: 18px;
        font-size: 1.05rem;
        background: #fafafa;
        transition: border 0.2s;
      }
      .update-email-card input[type="email"]:focus {
        border: 1.5px solid #ff512f;
        outline: none;
        background: #fff;
      }
      .update-email-card button[type="submit"] {
        width: 100%;
        padding: 10px 0;
        border: none;
        border-radius: 5px;
        background: linear-gradient(90deg, #ff512f 0%, #dd2476 100%);
        color: #fff;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s;
        margin-bottom: 10px;
      }
      .update-email-card button[type="submit"]:hover {
        background: linear-gradient(90deg, #dd2476 0%, #ff512f 100%);
      }
      .update-email-card .back-link {
        display: block;
        text-align: center;
        color: #ff512f;
        text-decoration: underline;
        font-size: 0.98rem;
        margin-top: 8px;
        opacity: 0.85;
        transition: opacity 0.2s;
      }
      .update-email-card .back-link:hover {
        opacity: 1;
        color: #dd2476;
      }
    </style>
    <div class="update-email-card">
      <h2>Update Email</h2>
      <form action="/updatemail" method="POST" autocomplete="off">
        <label for="email">New Email Address</label>
        <input type="email" id="email" name="email" required placeholder="Enter your new email" value="<%= typeof email !== 'undefined' ? email : '' %>">
        <button type="submit">Update Email</button>
      </form>
      <a class="back-link" href="/profile">Back to Profile</a>
    </div>
</body>
</html>
