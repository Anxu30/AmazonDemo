<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EditInfo</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<%-include('../partials/nav.ejs')%>
<div class="min-h-screen flex flex-col justify-center items-center bg-gradient-to-br from-indigo-200 via-blue-100 to-purple-200 py-10">
  <div class="bg-white bg-opacity-90 rounded-3xl shadow-2xl p-8 w-full max-w-md relative overflow-hidden">
    <div class="absolute -top-10 -right-10 w-32 h-32 bg-gradient-to-tr from-indigo-400 to-purple-300 opacity-20 rounded-full z-0"></div>
    <div class="absolute -bottom-10 -left-10 w-32 h-32 bg-gradient-to-bl from-blue-300 to-indigo-200 opacity-20 rounded-full z-0"></div>
    <h2 class="text-3xl font-extrabold text-indigo-700 mb-6 text-center z-10 relative">Edit Your Info</h2>
    <form class="flex flex-col gap-5 z-10 relative" action="/editInfo" method="post">
      <div class="flex flex-col items-center gap-2">
        <label for="url" class="text-sm font-semibold text-indigo-600">Profile Picture URL</label>
        <input 
          type="text" 
          name="url" 
          id="url" 
          class="rounded-full w-32 h-32 border-2 border-indigo-200 focus:border-indigo-400 transition p-2 text-center shadow-sm bg-gradient-to-br from-indigo-50 to-white"
          placeholder="Profile Picture URL"
          value="<%=meta?meta.profilePicUrl:''%>"
        >
        <% if (meta && meta.profilePicUrl) { %>
          <img src="<%= meta.profilePicUrl %>" alt="Profile Picture" class="w-24 h-24 rounded-full object-cover border-4 border-indigo-200 mt-2 shadow-md" />
        <% } %>
      </div>
      <div>
        <label for="name" class="block text-sm font-semibold text-indigo-600 mb-1">Name</label>
        <input 
          type="text" 
          name="name" 
          id="name" 
          class="w-full rounded-xl border-2 border-indigo-200 focus:border-indigo-400 transition p-3 bg-gradient-to-br from-indigo-50 to-white"
          placeholder="Your Name"
          value="<%=meta?meta.name:''%>"
        >
      </div>
      <div>
        <label for="email" class="block text-sm font-semibold text-indigo-600 mb-1">Email</label>
        <input 
          type="email" 
          name="email" 
          id="email" 
          class="w-full rounded-xl border-2 border-indigo-200 focus:border-indigo-400 transition p-3 bg-gradient-to-br from-indigo-50 to-white"
          placeholder="Your Email"
          value="<%=meta?meta.email:''%>"
          readonly
        >
      </div>
      <div>
        <label for="contact" class="block text-sm font-semibold text-indigo-600 mb-1">Contact</label>
        <input 
          type="number" 
          name="contact" 
          id="contact" 
          class="w-full rounded-xl border-2 border-indigo-200 focus:border-indigo-400 transition p-3 bg-gradient-to-br from-indigo-50 to-white"
          placeholder="Contact Number"
          value="<%=meta?meta.contact:''%>"
        >
      </div>
      <div>
        <label for="dob" class="block text-sm font-semibold text-indigo-600 mb-1">Date of Birth</label>
        <input 
          type="date" 
          name="dob" 
          id="dob" 
          class="w-full rounded-xl border-2 border-indigo-200 focus:border-indigo-400 transition p-3 bg-gradient-to-br from-indigo-50 to-white"
          value="<%=meta?meta.DOB:''%>"
        >
      </div>
      
      <div class="flex justify-between items-center mt-4 gap-4">
        <input 
          type="submit" 
          value="Save" 
          class="flex-1 bg-gradient-to-r from-indigo-500 to-purple-400 text-white font-bold py-2 rounded-xl shadow-md hover:from-indigo-600 hover:to-purple-500 transition cursor-pointer"
        >
        <a 
          href="/personal" 
          class="flex-1 text-center bg-gradient-to-r from-gray-200 to-indigo-100 text-indigo-700 font-semibold py-2 rounded-xl shadow hover:from-indigo-100 hover:to-gray-200 transition"
        >
          Back
        </a>
      </div>
    </form>
  </div>
</div>
</div>

</body>
</html>